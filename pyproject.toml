[project]
name = "medisync"
version = "1.0.0"
dependencies = []

[tool.ruff]
line-length = 88
# Ignora erros de imports não usados ou espaços se necessário
ignore = ["F401", "E402"] 
select = ["E", "F", "W"]
